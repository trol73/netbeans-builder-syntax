name = 'my=project'

src = [
   'src/*.c',
]

mcu = 'atmega8'

frequency = 16*1000000

defines = []

compiler_options = ['-g2  -Os']

linker_options = []

#configurations = {}

#bootloader_hex = 'bootloader.hex'